const SongItemLoader = ({ theme }) => {
  return (
    <div
      style={{ background: theme }}
      className="h-12 rounded-md animate-pulse opacity-20"
    ></div>
  );
};

export default SongItemLoader;
